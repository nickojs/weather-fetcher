import styled, { css } from 'styled-components';

export const WeatherContainer = styled.section`
  max-width: 400px;
  min-width: 300px;
  
  margin: 0 auto;
  padding: 40px 12px;
  text-align: center;
  
  background: #fafafa;
`;
